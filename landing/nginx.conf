server {
  listen 80;
  server_name _;
  root /usr/share/nginx/html;
  index index.html;

  # Single-page marketing site: everything resolves to the one document.
  location / {
    try_files $uri /index.html;
    add_header Cache-Control "no-cache";
  }
}
